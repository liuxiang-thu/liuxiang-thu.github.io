# 刘翔个人主页

双击 `index.html` 即可打开。网站文件与素材都在当前文件夹中，可整体复制或上传；不需要安装依赖或编译。

## 文件与上传

- `index.html`：主页入口，包含基本信息、论文介绍、摄影作品集三个独立章节。
- `content.js`：中英文内容、论文作者与链接、摄影分类和图片引用。
- `styles.css`、`app.js`：主页外观与交互。
- `assets/portraits/`：四张生活照。
- `assets/papers/`：六篇论文的 Fig.1。
- `assets/photos/`：压缩后的无水印摄影作品；网站与内容管理页均直接使用这些文件。
- `assets/awards/`：荣誉与获奖栏目使用的四张证书图片。
- `admin.html`、`admin.js`、`admin.css`：本地内容管理工具。

上传时让 `index.html` 位于网站根目录，并保留文件夹的相对结构。可以上传到支持静态 HTML 的托管空间，也可以放在一个网站的子目录；章节采用 hash 跳转，不依赖服务器重写规则。所有展示素材都已存入本地，没有外部字体或框架依赖。

## 编辑与照片上传

1. 点击主页右上角齿轮，或者双击 `admin.html`。
2. 管理页保留“预览”和“保存并导出”，不再使用草稿箱。保存导出的 `content.js` 本身就是可备份的内容文件；将它替换到网站文件夹，重新打开即可继续编辑。
3. 点击“返回主页”或左上角姓名时，如果有未导出的修改，会询问“保存并返回 / 不保存，返回 / 继续编辑”。关闭标签页也会提示未保存修改。
4. 研究标签、生活照路径可以添加、删除。个人经历、导师、硕士荣誉、本科荣誉支持添加、删除和 ▴ ▾ 排序；双语长文本左右并排。
5. 论文按第一 / 共同第一作者和合作论文分别管理。点击“置顶”后，论文移到所属类别前列并以浅蓝背景、深蓝边框、金色星星突出；可置顶多篇，使用 ▴ ▾ 调整置顶之间或普通论文之间的顺序。两个展示章节都会使用同样的顺序。
6. 摄影管理按“年份＋主题”分组，照片缩略图位于左侧，文字与参数位于右侧。用右上角“中文 / English”切换当前编辑语言。每张作品可点“置顶轮播 / 取消置顶”；多张置顶作品会按置顶顺序在主页摄影标题下方循环播放。
7. 可以全选、按组选择或单独选择照片，批量删除或移动到已有类别、新建类别；跨年移动不会修改图片内容。
8. 添加照片时先选择类别、年份，再选择 JPG / PNG / WebP 图片，可多选。上传会读取 JPEG / PNG 中可用的 EXIF 参数，缺少的焦距、ISO、快门和光圈可以手动填写。
9. 照片长边不超过 2560 像素，按所选目标压缩到单张 3 MB / 1 MB 以下。水印为“年份 / @橘子掉了一地”；背景由画面主色自动生成，文字按背景亮度选择黑或白。不能保证任意原图无损压至 1 MB，原始文件不受影响。
10. “保存并导出”下载 `content.js`，替换网站文件夹的同名文件，再重新上传。若浏览器重命名为 `content (1).js`，请改回 `content.js`。

照片底部和放大预览均按左侧参数、中间作品名、右侧下载链接排列，下载的是附带水印的展示图片。作品列表统一使用 3:2 展示框：横屏作品充满画框，竖屏作品按原比例缩放并居中；放大预览始终展示完整原比例。

`photo-tools.js` 提供本地参数读取、压缩和水印功能。干净底图以 Base64 存入内容文件，以便双击 HTML 离线打开时，也能安全重绘跨年水印而不重复叠加。单张体积限制针对生成的 JPEG；Base64 编码会增加约三分之一体积，底图备份也会增加内容文件体积。

## 登录和统计的实际范围

当前版本是可离线打开的静态网站，管理页不是在线后台，也不声称提供权限保护。任何人打开管理页都只能在自己的浏览器中编辑和下载副本，无法改动已经上线的文件。

GitHub / 微信登录后直接上传到服务器，需要部署后端、配置 OAuth 回调、在服务器校验站点所有者身份，并配置持久化存储。微信还需要相应开放平台应用配置。这些依赖尚未接入，因此本版本没有伪造登录按钮或账号校验。对于个人静态主页，当前的本地编辑—导出—上传流程更适合直接使用。

右上角的眼睛图标旁直接显示历史浏览人数，点击图标或人数仍会打开统计面板。尚未连接统计时显示“— / 暂未连接全站访客统计”，不会把本机刷新次数当成全站人数。管理页可分别填写记录访问和读取统计的 HTTPS 接口。读取接口返回格式为：

```json
{"uniqueVisitors":123}
```

当前推荐使用 `visitor-worker/` 中的 Cloudflare Worker + D1 实现。主页首次打开时向 `/visit` 提交保存在浏览器中的匿名 UUID，Worker 仅保存其 SHA-256 哈希；同一浏览器重复刷新不会增加人数。页面从 `/stats` 读取累计结果。摄影作品的爱心数据由 `/likes` 批量读取、由 `/like` 添加或取消；未点赞显示空心爱心，已点赞显示实心爱心，同一匿名访客对同一作品只保留一个点赞状态。主题内作品在每次打开或刷新页面时按爱心数从高到低排列。四个接口只允许 `https://liuxiang-thu.github.io` 跨域访问，不要在前端文件中填写服务端密钥。

更新已有 D1 数据库时，在 Cloudflare D1 控制台执行 `visitor-worker/schema.sql` 即可；其中的 `CREATE TABLE IF NOT EXISTS` 不会清空已有访客数据，只会补建 `photo_likes` 表和索引。随后把 `visitor-worker/src/index.js` 部署到现有 Worker。

## 内容核对与尚待补充

- 基本资料、实习经历和研究生团队荣誉来自提供的国家奖学金申请表。申请表不是获奖证明，因此没有将国家奖学金写为已获得。
- 数学竞赛国一等奖为 2024 年 4 月；数学建模江苏赛区一等奖按竞赛官网获奖名单发布时间记为 2023 年 11 月，获奖人是刘翔、曾宇、钟世贵。科创之星证书落款为 2024 年 11 月、全校共 10 名。
- 教育经历采用本科 2021—2025、硕士 2025—至今；智源实习自 2026 年 3 月开始。
- ConfAL-WM、LPA-CWM、GeoCo-SAVi 和 HamiFormer 的作者顺序采用用户提供的 ICLR 截图。公开 arXiv 版本可能仍是旧名单，主页按截图展示，不反向改写原论文。
- `*` 表示第一 / 共同第一作者；`†` 表示通讯，`‡` 表示通讯兼项目负责人。可确认的标记来自原论文。HamiFormer 暂未确认通讯与项目负责人，未擅自补标。
- 初始顺序按首次公开时间倒序，之后以管理页手动顺序及置顶设置为准；LPA-CWM 的 arXiv 首次公开时间为 2026-09-12。HamiFormer 暂按 2026 年 9 月归档，具体日期待补。
- HamiFormer 使用用户提供的 2026-09-17 初稿概述双专家扩散场、PLAS 仿射辛映射与 Regime Model Tree，并展示用户提供的论文 Fig.1。
- GeoCo-SAVi 原论文给出的项目主页在核对时返回 404，故只保留 arXiv 链接；MOSH-WM 未找到可用独立项目页，也只保留 arXiv。
- 摄影年份按 EXIF 核对：天安门为 2026，火山主题为 2025。作品名称为本次展示拟定，可在管理页修改。摄影页顶部固定展示 13 张手动置顶作品；西藏风光位于 2025 年首个主题。

## 内容来源

- 张长水：[清华大学自动化系](https://www.au.tsinghua.edu.cn/info/1110/1575.htm)
- 崔森：[个人主页](https://sencui-thu.github.io/)
- Scene2Demo：[arXiv](https://arxiv.org/abs/2602.12065) · [项目主页](https://scene2demo-anon.github.io/)
- ConfAL-WM：[arXiv](https://arxiv.org/abs/2608.25572) · [项目主页](https://ConfAL-WM.github.io/)
- LPA-CWM：[arXiv](https://arxiv.org/abs/2609.14073) · [项目主页](https://LPA-CWM.github.io/) · [原提供的预印本](https://doi.org/10.5281/zenodo.22733005)
- GeoCo-SAVi：[arXiv](https://arxiv.org/abs/2609.06628)
- MOSH-WM：[arXiv](https://arxiv.org/abs/2608.22750)
- HamiFormer：[OpenReview](https://openreview.net/forum?id=Xh36ESRChj)

已检查桌面与手机布局、独立章节跳转、浏览器前进后退支持、中英文、主题切换、轮播、图片放大、证书弹窗、文件方式打开、管理页增删排序、返回确认、置顶预览、照片批量移动、参数读取与压缩水印。源码和图片未包含原始申请表或证件号码；`assets/awards/` 中只放置了用户指定的四张获奖证明。

合成图、视频帧和社交平台导出图若无法从同级原片补齐 EXIF，参数以“—”显示，未编造数值。
